@model Comment

@* <p>Are you sure you want to delete this comment?</p>
<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
 *@

@* <form id="commentDeleteForm" method="post" action="@Url.Action("Delete", "Comment", new { id = Model.Id })">
    @Html.AntiForgeryToken()
    <h5>Are you sure you want to delete this comment?</h5>
    <div class="form-group">
        <label>Comment Text: @Model.commentText</label>
    </div>
    <div class="form-group">
        <button type="button" onclick="submitDeleteForm()" class="btn btn-danger">Delete</button>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
    </div>
</form> *@